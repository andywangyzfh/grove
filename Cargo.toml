[package]
name = "grove-rs"
version = "0.11.3"
edition = "2021"
description = "A Kanban-style TUI for parallel AI coding. Manage Git worktrees as tasks, run multiple agents in isolated tmux sessions, get notified via hooks."
license = "MIT"
repository = "https://github.com/GarrickZ2/grove"
homepage = "https://github.com/GarrickZ2/grove"
keywords = ["git", "worktree", "tmux", "tui", "cli"]
categories = ["command-line-utilities", "development-tools"]
readme = "README.md"
include = [
    "/Cargo.lock",
    "/Cargo.toml",
    "/LICENSE",
    "/README.md",
    "/CHANGELOG.md",
    "/build.rs",
    "/install.sh",
    "/package-lock.json",
    "/package.json",
    "/tauri.conf.json",
    "/docs/apple-touch-icon.png",
    "/docs/favicon.png",
    "/docs/favicon.svg",
    "/docs/logo.png",
    "/gen/**",
    "/grove-web/dist/**",
    "!/grove-web/dist/.DS_Store",
    "/grove-extension/dist/**",
    "!/grove-extension/dist/.DS_Store",
    "/scripts/**",
    "/src/**",
    "/src-tauri/**",
]

[[bin]]
name = "grove"
path = "src/main.rs"

[features]
default = []
gui = ["dep:tauri", "dep:tauri-plugin-shell", "dep:tauri-plugin-global-shortcut", "dep:tauri-build", "dep:rfd", "dep:tauri-plugin-updater", "dep:tauri-plugin-deep-link"]
# Enables /api/v1/perf/sysinfo (process RSS + CPU%) for the perf-build
# frontend. Off by default — release ships without sysinfo dep or handler.
perf-monitor = ["dep:sysinfo", "dep:tracing", "dep:tracing-subscriber"]

[dependencies]
ratatui = "0.29"
crossterm = "0.28"
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
dirs = "5"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4", features = ["derive"] }
rpassword = "7"
arboard = "3"
ureq = { version = "2.9", features = ["json"] }
semver = "1.0"
once_cell = "1.19"
serde_json = "1.0"
notify = "6.1"
thiserror = "2.0"
cron = "0.12"
glob = "0.3"
globset = "0.4"
walkdir = "2"
rayon = "1.10"
encoding_rs = "0.8.35"

# Symbol indexing (cmd+click navigation). Tree-sitter grammar bundled
# statically; only Go is wired up for now.
tree-sitter = "0.25"
tree-sitter-go = "0.25"

# MCP Server
# transport-streamable-http-server is used by the Agent Graph in-process MCP listener
# (src/api/handlers/agent_graph_mcp.rs). The stdio `grove mcp` uses transport-io.
rmcp = { version = "1.3", features = ["server", "transport-io", "transport-streamable-http-server"] }
schemars = "1.0"
tokio = { version = "1", features = ["full"] }

# Web API Server
axum = { version = "0.8", features = ["ws", "multipart"] }
tower-http = { version = "0.6", features = ["cors", "fs"] }
rust-embed = "8"
mime_guess = "2"
open = "5"

# Terminal WebSocket
portable-pty = "0.8"
futures = "0.3"

# Marketplace binary install (P3): download + extract per-platform archives.
# Using plain `flate2 + tar` instead of async variants because install runs
# inside `tokio::task::spawn_blocking` and benefits from the established
# blocking IO ergonomics. `zip` covers windows .zip releases.
flate2 = "1"
tar = "0.4"
zip = { version = "2", default-features = false, features = ["deflate"] }

# ACP (Agent Client Protocol)
agent-client-protocol = { version = "0.12", features = ["unstable_session_model", "unstable_session_usage", "unstable_session_fork", "unstable_session_delete"] }
tokio-util = { version = "0.7", features = ["compat"] }
async-trait = "0.1"
tokio-stream = "0.1"
tokio-tungstenite = { version = "0.26", default-features = false, features = ["connect", "rustls-tls-native-roots"] }

# GUI (Tauri) - optional
tauri = { version = "2", optional = true , features = ["devtools", "tray-icon", "image-png"] }
tauri-plugin-shell = { version = "2", optional = true }
tauri-plugin-global-shortcut = { version = "2", optional = true }
tauri-plugin-updater = { version = "2", optional = true }
tauri-plugin-deep-link = { version = "2.0.0-rc", optional = true }
rfd = { version = "0.15", optional = true }
sysinfo = { version = "0.32", optional = true }
tracing = { version = "0.1", optional = true }
tracing-subscriber = { version = "0.3", optional = true, features = ["registry"] }
rusqlite = { version = "0.34", features = ["bundled"] }
similar = "2.7.0"
getrandom = "0.2"
qrcode = "0.14"

# HMAC-SHA256 auth + TLS
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
rcgen = { version = "0.13", features = ["pem"] }
axum-server = { version = "0.7", features = ["tls-rustls"] }
rustls = { version = "0.23", default-features = false, features = ["ring"] }
base64 = "0.22"
url = "2"

# `grove mcp-bridge` 子命令需要：HTTP 客户端 + SSE stream 解码。
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls", "stream"] }
futures-util = "0.3"

# Read macOS Info.plist files in-process (avoid forking `defaults`).
plist = "1"

# In-process git for hot read paths (avoid fork+exec). Write/network ops still use CLI.
gix = { version = "0.66", default-features = false, features = ["max-performance-safe", "revision"] }

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[target.'cfg(windows)'.dependencies]
junction = "1.2"

[build-dependencies]
tauri-build = { version = "2", features = [], optional = true }

[dev-dependencies]
tempfile = "3"
# reqwest 已在 [dependencies] 声明（runtime 用），dev-dependencies 不再重复

# Config for 'cargo dist'
[workspace.metadata.dist]
# CI backends to support
ci = "github"
# The preferred cargo-dist version to use in CI
cargo-dist-version = "0.27.0"
# Target platforms to build for
targets = [
    "aarch64-apple-darwin",
    "x86_64-apple-darwin",
    "x86_64-unknown-linux-musl",
    "aarch64-unknown-linux-musl",
    "x86_64-pc-windows-msvc",
    "aarch64-pc-windows-msvc",
]
# Which actions to run on pull requests
pr-run-mode = "skip"
# Whether to install an updater program
install-updater = false

# macOS-only deps (used by tray module to call NSApp.activateIgnoringOtherApps).
[target.'cfg(target_os = "macos")'.dependencies]
objc2 = { version = "0.5" }

# Profile used by `make flamegraph`: release optimizations + line-number
# debuginfo so samply can symbolicate frames. Doesn't affect normal `cargo
# build --release` (release ships from dist-workspace anyway).
[profile.release]
debug = "line-tables-only"
